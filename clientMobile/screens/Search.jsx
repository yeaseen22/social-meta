import { View, Text } from 'react-native';

const Search = () => {
    return (
        <View style={{
            textAlign: 'center',
        }}>
            <Text>Search Screen</Text>
        </View>
    );
};

export default Search;
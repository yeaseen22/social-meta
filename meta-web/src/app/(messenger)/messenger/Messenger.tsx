import React from 'react';
import { MessagesChat } from '@/components/messenger';

const Messenger = () => {
  return (
    <React.Fragment>
      <MessagesChat />
    </React.Fragment>
  );
};

export default Messenger;
